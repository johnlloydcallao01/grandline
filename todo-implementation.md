We must respect and rely to my CMS online hosted in vercel because that is my centralized backend api provider:

NEXT_PUBLIC_API_URL=https://grandline-cms.vercel.app/api

Are we doing it? Also, why it starts "Signing in" instead of waiting 